public class Villa extends Property {
    public Villa(){
    }
    public Villa(String title, double price, int size, int roomCount, int hallCount) {
        super(title, price, size, roomCount, hallCount);
    }
}
